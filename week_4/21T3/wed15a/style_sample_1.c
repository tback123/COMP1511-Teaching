#include <stdio.h>

int main(void) {
    // Declaring Variables
    int primary = 7; 
    int high = 12; 

    // Reading in from the user
    printf("What year are you in? ");
    int year = 20;
    scanf("%d", &year);

    // Check if user in high school
    if (year > primary || year == a) {
        if (year < high || year == high) {
            printf("You are in high school\n");
        }
    }

    // Check if other
    if ( year < 7 || year > 12) {
        printf("You are not in high school\n");
    }

    return 0;
}