// A program to demonstrate arrays

#include <stdio.h>

#define SIZE 7

int main (void) {

    int toms_age = 20;

    int class_ages[SIZE] = {0};

    grid[2] = 100;

    int counter = 0;
    while (counter < SIZE) {
        printf("Index %d has value: %d\n", counter, class_ages[counter]);
        counter++;
    }

    return 0;
}

