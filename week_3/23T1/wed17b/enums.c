// A program to show the use case of a loop
// Written by Tom's COMP1511 Tutorial

#include <stdio.h>

enum days {
    MON,
    TUE,
    WED,
    THU,
    FRI
};

int main(void) {

    enum days my_day = MON;

    if (num == MON) {
        printf("MONDAY\n");
    }

    return 0;
}